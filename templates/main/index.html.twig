{% extends 'base.html.twig' %}

{% block body %}
<main class="container">
    <section class="row">
        <div class="col-12">
            <h1>CandyShop</h1>
            <p>Bienvenue dans notre boutique</p>
        </div>
        {% for category in categories %}
            <div class="col-4">
                <div class="card mb-3" h-100>
                    <div class="card-body">
                        <h3 class="card-title">{{ category.name }}</h3>
                        <a href="{{ path('categories_list', {slug: category.slug}) }}" class="btn btn-primary">Voir les bonbons</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </section>
</main>
{% endblock %}
